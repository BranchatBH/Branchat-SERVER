spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  web:
    resources:
      add-mappings: false
  data:
    redis:
      password: ${REDIS_PASSWORD}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
logging:
  level:
    root: DEBUG
